import 'package:url_strategy/url_strategy.dart';

class StringSimilarity extends StatelessWidget {
  const StringSimilarity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
      TextEditingController stringController = TextEditingController();
        TextEditingController string2Controller = TextEditingController();


 


    return Scaffold(
      bod: Column(
        children: [

        ], 
      ),
    );
  }
}